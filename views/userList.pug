html
    head
        h1 UserList
    body 
        if users.length>0
            table

                tr
                    td name 
                    td password 
                    td gender
                    td isAdmin
                each user in users 
                    tr 
                        td #{user.name}
                        td #{user.password}
                        td #{user.gender}
                        td #{user.isAdmin}
                        td 
                            a(href=`/admin/edituser/${user.id}`) edit
                    

        else 
            h1 No users available 
